[
    {
        "awsAccount": {
            "awsAccountId": "${ACCOUNT_ID}"
        },
        "awsRegion": {
            "regionName": "${AWS_REGION}"
        },
        "deploymentMode": "ON_CREATE",
        "deploymentOrder": 0,
        "description": "Configuration for the Tooling",
        "environmentBlueprintId": "${TOOLING_ID}",
        "name": "Tooling"
    },
    {
        "awsAccount": {
            "awsAccountId": "${ACCOUNT_ID}"
        },
        "awsRegion": {
            "regionName": "${AWS_REGION}"
        },
        "configurationParameters": {
            "parameterOverrides": [
                {
                    "isEditable": true,
                    "name": "glueDbName",
                    "value": "custom_glue_db"
                }
            ]
        },
        "deploymentMode": "ON_CREATE",
        "deploymentOrder": 1,
        "description": "Creates databases in Amazon SageMaker Lakehouse for storing tables in S3 and Amazon Athena resources for your SQL workloads",
        "environmentBlueprintId": "${DATALAKE_ID}",
        "name": "Lakehouse Database"
    },
    {
        "awsAccount": {
            "awsAccountId": "${ACCOUNT_ID}"
        },
        "awsRegion": {
            "regionName": "${AWS_REGION}"
        },
        "deploymentMode": "ON_CREATE",
        "deploymentOrder": 1,
        "description": "...",
        "environmentBlueprintId": "${WORKFLOW_ID}",
        "name": "Workflow"
    }
]
